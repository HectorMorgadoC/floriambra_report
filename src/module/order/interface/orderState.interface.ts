export enum OrderState {
    Esperando = "not_starter",
    Progresando = "in_progress",
    Culminado = "done",
}

export enum OrderStateOptional {
    Esperando = "not_starter",
    Progresando = "in_progress",
}